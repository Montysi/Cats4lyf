import React, { useContext } from "react";
import styled from "styled-components";
import { BasketContext } from "./context/BasketContext";

constBasketModal =()= {
    const {showModal, closeModal, basketItems}
}
